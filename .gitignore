# --- Python backend ------------------------------------------------------
.venv/
config.json
data/
dist/
build/
*.spec
__pycache__/
.pytest_cache/
installer/redist/*.zip

# --- Node / frontend -------------------------------------------------------
# Applies to the root (MCP tooling deps) and any nested project (frontend/).
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# Vite / frontend build output (generated, not committed).
# poetry run build-exe copies this to dist/webui/ next to the exe.
frontend/dist/

# --- Editors / OS ------------------------------------------------------
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# .cursor/ holds local MCP config and rules; keep rules, ignore local state.
.cursor/*
!.cursor/rules/
!.cursor/mcp.json
